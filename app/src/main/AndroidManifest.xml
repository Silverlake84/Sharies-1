<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.esiea.tim.sharies">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

    <activity android:name=".MainActivity">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>

    <activity
        android:name="com.esiea.tim.sharies.ProfilActivity"
        android:parentActivityName=".MainActivity" />

    <activity
        android:name="com.esiea.tim.sharies.creditActivity"
        android:parentActivityName=".MainActivity" />

    <activity android:name="com.esiea.tim.sharies.SecondeActivity" />

    <activity android:name="com.esiea.tim.sharies.classtest" />

    <service
        android:name="com.esiea.tim.sharies.GetSeriesServices"
        android:exported="false"></service>
    </application>


</manifest>
